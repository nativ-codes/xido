import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    options: {
        marginHorizontal: 16,
        gap: 8
    },
    buttons: {
        margin: 16,
        gap: 8
    }
});