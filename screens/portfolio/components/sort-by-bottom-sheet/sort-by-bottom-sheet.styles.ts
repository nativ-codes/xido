import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    options: {
        marginHorizontal: 16,
        gap: 8
    },
    buttons: {
        marginHorizontal: 16,
        marginTop: 15,
        gap: 8
    }
});