import { StyleSheet } from "react-native";

export default StyleSheet.create({
    cardsWrapper: {
        marginHorizontal: 16,
        gap: 16
    },
    footer: {
        alignItems: 'center',
        marginTop: 16        
    }
});