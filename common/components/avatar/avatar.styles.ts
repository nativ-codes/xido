import {StyleSheet} from 'react-native';

const avatarSize = 40;

export default StyleSheet.create({
    avatar: {
        width: avatarSize,
        height: avatarSize,
        borderRadius: avatarSize / 2
    }
});